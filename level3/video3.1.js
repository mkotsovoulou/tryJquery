$(document).ready(function() {
    // create a new p node with the price
    var price = $('<p class="price">From $199 </p>');
    // place the price node at the bottom of .vacation
    $(".vacation").append(price);
    //remove the button
    
    
});