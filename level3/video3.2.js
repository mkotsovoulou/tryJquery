$(document).ready(function() {
  //Change the code below so that it works only when the button is clicked

       var price = $('<p class="price">From $199 </p>');
       $(".vacation").append(price);
       $("button").remove();
  
    
});